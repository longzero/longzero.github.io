---
templateClass: template-map
---

<!doctype html>
<html lang="{{ metadata.language }}">
  {% include 'partials/head-map.njk' %}

  {# TODO: Move map CSS into map.css if CSS gets too big. #}
  {# For now, it is inline <style> in the page and that is just fine. You just saved a server request. #}
  {# <link rel="stylesheet" href="{{ '/assets/css/map.css' | url }}"> #}

  <body{% if templateClass %} class="{{ templateClass }}"{% endif %}>
    {% include 'partials/header.njk' %}

    <main>

      {# <!-- Delete this message -->
      <div class="infobox">
        <ol>
          <li>Edit the <code>_data/metadata.json</code> with your blog’s information.</li>
          <li>(Optional) Edit <code>.eleventy.js</code> with your <a href="https://www.11ty.dev/docs/config/">configuration preferences</a>.</li>
          <li>Delete this message from <code>_includes/layouts/base.njk</code>.</li>
        </ol>
        <p><em>This is an <a href="https://www.11ty.dev/">Eleventy project</a> created from the <a href="https://github.com/11ty/eleventy-base-blog"><code>eleventy-base-blog</code> repo</a>.</em></p>
      </div>
      <!-- Stop deleting --> #}

      {{ content | safe }}

    </main>

    <script>
      window.va = window.va || function () { (window.vaq = window.vaq || []).push(arguments); };
    </script>
    <script defer src="/_vercel/insights/script.js"></script>

    <script src="{{ '/assets/js/map/map.js' | url }}"></script>

    <!-- Current page: {{ page.url | url }} -->

  </body>
</html>
